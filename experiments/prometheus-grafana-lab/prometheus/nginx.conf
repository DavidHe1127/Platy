http {
    server {
        listen              443 ssl;
        server_name         nginx;
        ssl_certificate     /etc/certs/prometheus.crt;
        ssl_certificate_key /etc/certs/prometheus.key;

        location /federate/ {
            proxy_pass http://prometheus:9090/;
        }
    }
}

events {}
